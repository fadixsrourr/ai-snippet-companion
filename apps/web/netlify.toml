[build]
  base = "apps/web"        # run everything from apps/web
  command = "npm run build"
  publish = "dist"         # relative to base => apps/web/dist

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
